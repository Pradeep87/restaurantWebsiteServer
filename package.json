{
  "name": "restaurantwebsite",
  "version": "1.0.0",
  "description": "this is the backend for restaurant website",
  "main": "server.js",
  "proxy": "http://localhost:4000",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon backEnd/server",
    "client": "npm start --prefix restaurantWebsite ",
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "client-deps": "cd restaurantWebsite && npm install"
  },
  "author": "pradeep",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.2.1",
    "express": "^4.18.1",
    "mongoose": "^6.3.5",
    "nodemon": "^2.0.16",
    "swagger-ui-express": "^4.4.0"
  }
}
